apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kanrigate.fullname" . }}-config
  labels:
    {{- include "kanrigate.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
